'use strict';

module.exports = {
  // To reduce "pop" sounds when gain adjusts, take this time to linearly adjust
  GAIN_ADJUSTMENT_RAMP_MS: 16,

  // Length of a morse "dit"
  MORSE_INTERVAL_MS: 100,

  // Size of the cranes image in pixels
  IMAGE_WIDTH: 4032,
  IMAGE_HEIGHT: 2548
};
